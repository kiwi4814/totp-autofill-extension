# todos.md

本文档记录 Local TOTP Autofill 的后续优化事项。它只保留**尚未完成或仍需深化**的工作；已经落地的功能统一归入“当前基线”，避免 TODO 与代码状态脱节。

## 优先级说明

- **P0**：下一阶段优先做，直接影响可用性、安全边界或用户能否顺利完成 2FA。
- **P1**：高价值增强，适合作为第二批功能。
- **P2**：体验、工程化、发布或长期维护优化。
- **P3**：可选探索，不阻塞核心可用性。

## 术语约定

后续文档和 UI 文案优先使用这些中文术语，必要时只在技术说明中补充英文协议名或字段名：

- **条目**：一组 TOTP 配置。
- **服务商**：条目的服务来源，例如 GitHub、Google。
- **名称**：账号、邮箱或用户可识别的条目名称。
- **备注**：用户补充说明。
- **匹配域名**：用于自动匹配当前网站的域名别名。
- **当前网站**：Popup 正在处理的活动标签页域名。
- **绑定当前网站**：把当前网站加入某个条目的匹配域名。
- **验证码**：当前生成的 TOTP 数字码。
- **主密码**：用户可选开启的本地加密口令。
- **本地存储**：`chrome.storage.local`。
- **导入向导**：带来源选择、预览和确认的导入流程。

## 当前基线（代码已实现）

- Chrome Manifest V3 扩展，权限保持为 `activeTab`、`scripting`、`storage`，不声明长期站点权限。
- 本地导入和生成验证码：Aegis 明文 JSON、Aegis 加密 JSON、Google Authenticator 迁移内容、单条 `otpauth://totp/...`。
- 设置页支持 Aegis 文件导入、文本导入、Google Authenticator 二维码图片识别、导入预览后确认写入、搜索、编辑服务商/名称/备注/匹配域名、删除单条、清空全部。
- Popup 支持读取当前网站、自动匹配条目、显示验证码和倒计时、复制验证码、填充当前页面。
- 当前网站无匹配时，Popup 已支持按服务商/名称/匹配域名搜索，选择条目后绑定当前网站并立即填充；当前网站匹配多个条目时，也支持搜索缩小范围。
- Popup 已增加倒计时进度和低剩余时间提示。
- 设置页已增加安全状态面板，明确不联网、无长期站点权限和当前便利模式风险。
- 设置页条目管理已从可编辑卡片堆叠改为“条目列表 + 当前条目详情编辑”。
- 自动填充支持常规单个验证码输入框、多格单字符验证码输入框、带强验证码特征的密码输入框，并避免填入普通密码框。
- 填充失败时已有基础错误文案和复制手动粘贴兜底。
- 匹配逻辑已支持域名标准化、常见登录前缀去除、匹配域名优先、服务商名称模糊匹配。
- 测试已覆盖核心导入、生成、匹配、存储更新、Popup 无匹配搜索绑定、基础填充策略、设置页加密导入重试、导入预览确认和设置页列表 / 详情面板。

---

## P0：下一阶段优先专题

### 1. UI / 交互友好化专题（Claude Design）

**状态**：进行中（已完成设计画布、Popup 多状态搜索入口、低剩余时间提示、设置页安全状态面板、设置页“列表 + 详情面板”；剩余危险操作隔离、可访问性细节和真实浏览器视觉检查）。

**设计画布**：[`docs/design/local-totp-ui-redesign.html`](./docs/design/local-totp-ui-redesign.html)

**目标**：把扩展从“功能可用”推进到“用户打开后立即知道下一步该做什么”。Popup 聚焦当前网站的主操作；设置页聚焦条目管理、导入安全感和危险操作隔离。

**设计原则**：

- 一屏一主动作：已匹配时突出“立即填充当前网站”，未匹配时突出“绑定当前网站并填充”，无条目时突出“去导入”。
- 错误给出下一步：每个失败状态都说明原因，并保留复制验证码、手动粘贴或手动选择输入框等兜底路径。
- 安全状态常驻：设置页明确展示本地存储、是否启用主密码、是否联网、是否声明长期站点权限。
- 危险操作降噪：删除、清空、明文导出放入危险区域，必须二次确认。
- 文案统一中文术语：服务商、名称、备注、匹配域名、当前网站、绑定当前网站、验证码。

**已完成切片**：

- Popup 智能主页第一阶段：已匹配 1 个、匹配多个、无匹配、无条目等状态已有不同首屏和主动作。
- 倒计时体验第一阶段：已用进度条表达剩余时间，低剩余时间提示“即将刷新”。
- 设置页信息架构第一阶段：已拆为导入区、安全状态区、条目列表和当前条目详情编辑。
- 导入交互第一阶段：已改为预览后确认写入。

**剩余待做**：

1. 危险操作隔离：把删除单条、清空全部、未来明文导出集中到独立危险区域，并增加更明确的二次确认文案。
2. 交互反馈统一：复制、填充、绑定、保存、删除、导入完成统一成同一套成功 / 错误 / 可恢复提示。
3. 可访问性检查：按钮触达区域不小于 44px；键盘焦点清晰；状态和错误文案可被读屏理解。
4. 真实浏览器视觉检查：在 Chrome 扩展环境里检查 Popup 小尺寸、设置页响应式和深色模式。

**验收标准**：

- 用户打开 Popup 后，首屏即可判断当前网站是否匹配以及下一步该点什么。
- 自动填充失败时，用户仍能通过复制或手动路径完成 2FA。
- 设置页编辑条目时不需要在大量卡片之间滚动寻找。
- UI 文案不混用旧术语，敏感信息和危险操作有明确边界。
- 不新增扩展运行时依赖，不扩大权限范围。

### 2. 导入向导和冲突预览

**状态**：进行中（已完成导入预览后确认写入；剩余更细冲突分类、无效条目逐条展示和来源选择向导）。

**目标**：导入前让用户看见影响范围，避免重复导入、误覆盖或部分失败造成困惑。

**建议流程**：

1. 选择来源：Aegis JSON、Google Authenticator 二维码图片、迁移文本、单条 `otpauth://`。
2. 读取内容：加密 Aegis 仅在本地使用密码解密，明确密码不会保存。
3. 预览条目：标记新增、重复、将覆盖、无效、缺少服务商/名称的条目。
4. 用户确认：确认后才写入本地存储。
5. 结果反馈：展示成功数量、跳过数量、失败原因和下一步建议。

**验收标准**：

- 导入前能看到将新增、跳过或覆盖哪些条目。
- 单条失败不影响其他有效条目导入。
- 重复条目不会静默覆盖，用户能理解覆盖依据。
- 密码错误、格式错误、二维码无法识别都有可操作提示。

### 3. 可选主密码 / 加密存储

**目标**：在保留当前便利模式的同时，为需要更高安全边界的用户提供本地加密保护。

**建议设计**：

- 默认保持当前便利模式，避免破坏现有使用流程。
- 用户可在设置页开启主密码，并把已有条目迁移到加密存储。
- 使用 WebCrypto 派生密钥并加密验证码密钥。
- 解锁后短时缓存，不长期保存主密码。
- 关闭加密存储时必须明确风险确认。
- 与导出 / 备份专题共用加密能力，避免两套实现。

**验收标准**：

- 不破坏当前本地数据。
- 主密码不会保存到本地存储。
- 错误密码不会损坏已有数据。
- 锁定状态下不会生成或展示验证码。

### 4. 填充失败后的完整兜底机制

**状态**：已有基础错误文案和复制兜底，仍需补齐手动路径。

**目标**：把“没反应”变成明确可恢复的流程。

**建议功能**：

- 更细错误分类：无法读取当前网站、无法注入页面、没有找到验证码输入框、页面拒绝填充、验证码即将刷新。
- 手动选择输入框后填充：用户在页面中选择输入框，再由扩展填入当前验证码。
- 逐位填充模式：针对没有 `maxlength=1` 或 `size=1` 的多格验证码输入框。
- 失败后仍保留“复制验证码备用”。

**验收标准**：

- 自动填充失败时，用户仍可完成 2FA。
- 错误提示不暴露验证码密钥。
- 不新增长期站点权限。

---

## P1：高价值功能增强

### 5. 导出 / 备份 / 恢复

**目标**：避免误删或换设备时无法恢复本地整理好的条目和匹配域名。

**建议功能**：

- 导出为本项目 JSON 格式。
- 可选导出加密备份。
- 导入本项目备份并恢复服务商、名称、备注、匹配域名。
- 导出前明确提示验证码密钥敏感性。

**验收标准**：

- 用户可以完整备份和恢复条目数据。
- 明文导出必须有风险提示和二次确认。
- 加密导出复用主密码 / 加密存储能力，不重复造轮子。

### 6. 反向导出

**目标**：支持迁移到其他 2FA 工具，避免数据被锁在本扩展里。

**建议功能**：

- 导出为 Aegis 兼容 JSON。
- 导出为 `otpauth://` URI 列表。
- 可选加密导出。
- 导出前提示验证码密钥敏感性。

**验收标准**：

- 导出的 Aegis JSON 可被 Aegis Authenticator 识别。
- 导出的 `otpauth://` URI 可被常见 TOTP 工具识别。
- 不导出本扩展内部无意义的临时状态。

### 7. 更强的复杂页面填充能力

**目标**：覆盖更多真实网站 2FA 页面。

**建议功能**：

- Shadow DOM 输入框探测。
- iframe 内输入框探测（仅在权限允许范围内）。
- 多格验证码输入框智能分组：支持没有 `maxlength=1` 或 `size=1` 的相邻短输入框。
- 用户手动选择输入框后填充。
- 候选输入框评分说明，方便调试和测试。

**验收标准**：

- 不增加长期站点权限。
- 自动失败时有手动兜底。
- 常见银行、政务、云服务网站的 6 位验证码输入框可以正确填充或给出可用兜底。

### 8. 最近使用和常用条目

**目标**：让常用条目更容易触达，尤其是当前网站匹配多个条目时。

**建议功能**：

- 记录最近填充时间。
- 记录当前网站最近使用的条目。
- Popup 排序优先显示当前网站最近使用和常用条目。
- 用户可清除使用历史。

**验收标准**：

- 只记录域名级别信息，不记录页面路径。
- 用户可清除使用历史。
- 最近使用不影响手动搜索和绑定逻辑。

---

## P2：测试、工程化和发布

### 9. 拆分并测试浏览器填充逻辑

当前 `src/core/autofill-planner.js` 已覆盖部分纯逻辑，但 `src/content/autofill.js` 仍有真实 DOM、事件派发和页面注入逻辑未被自动化验证。

**建议**：

- 继续把候选评分和填充策略抽为纯函数。
- 增加 DOM fixture 测试或浏览器端测试。
- 覆盖多格输入、密码型验证码输入框、隐藏输入框、disabled / readOnly、Shadow DOM 等场景。

### 10. 浏览器端 smoke test

**建议工具方向**：

- Playwright 或 Chrome extension test harness。
- 本地测试页面模拟常见验证码表单。
- 验证 Popup -> 注入脚本 -> 填充表单闭环。

**注意**：引入测试依赖前单独评估；扩展运行时代码继续保持零第三方依赖。

### 11. CI

**建议 GitHub Actions**：

- `npm test`
- `npm run check`
- 可选：打包产物校验

**验收标准**：

- PR / push 后自动验证核心逻辑。
- CI 失败时能快速定位是测试、语法检查还是打包问题。

### 12. 打包和发布流程

**建议**：

- 增加 `npm run package` 生成 zip。
- 校验 zip 不包含 `.git`、`.omx`、`.DS_Store`、测试临时文件。
- 准备 Chrome Web Store 上架材料。
- 整理版本号策略、changelog 和 release notes。

### 13. 图标、截图和项目展示素材

**建议补充**：

- 扩展图标。
- Popup 小尺寸视觉适配截图。
- README 截图或 GIF。
- Chrome Web Store 宣传图所需素材。

### 14. 本地化和语言整理

**状态**：当前代码 UI 和错误文案已统一为中文；后续重点是上架所需本地化结构。

**建议功能**：

- 保持中文术语表与 UI 文案一致。
- 若准备上架，再补充 `/_locales`。
- 可选英文界面，但不要在当前中文 UI 中混用英文字段名。

---

## P3：可选探索

### 15. WebDAV 加密同步

**目标**：通过用户自有 WebDAV 同步数据，让手动整理的匹配域名不会因换设备或重装丢失。

**前置条件**：应先完成主密码 / 加密存储和备份格式，否则同步会扩大明文验证码密钥风险。

**建议功能**：

- 配置 WebDAV 服务器地址、用户名、密码。
- 同步数据使用主密码加密后上传，服务端无法读取明文。
- 支持手动触发同步和可选定时同步。
- 冲突处理：以本地为准、以远端为准或提示用户选择。

**验收标准**：

- 数据在传输和远端存储中均为加密状态。
- 不破坏现有本地存储结构。
- 用户能随时关闭同步并清除同步凭据。

### 16. 自动站点建议

根据服务商、名称、备注和匹配域名文本，给出可能匹配的域名建议。必须保持本地计算，不使用联网查询。

### 17. 高级数据清理

**建议功能**：

- 清除所有条目。
- 清除使用历史。
- 清除全部匹配域名。
- 清除导入暂存状态。
- 清除同步凭据（如果未来实现同步）。

---

## 推荐下一步实施顺序

1. **危险操作隔离和确认文案**：删除单条、清空全部、未来明文导出统一进入危险区域。
2. **导入向导第二阶段**：补充来源选择、逐条冲突分类、无效条目原因展示。
3. **填充失败完整兜底**：手动选择输入框、逐位填充模式和更细错误分类。
4. **可选主密码 / 加密存储**：建立后续备份、导出和同步的安全基础。
5. **导出 / 备份 / 恢复**：保护用户已整理好的本地数据。
6. **浏览器端 smoke test 和可访问性检查**：验证 Popup -> 注入脚本 -> 填充表单闭环。
7. **CI、打包和发布流程**：为稳定迭代和上架做准备。
