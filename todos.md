# todos.md

本文档记录 Local TOTP Autofill 的后续优化事项。优先级含义：

- **P0**：下一阶段优先做，直接影响可用性或安全边界。
- **P1**：高价值增强，适合作为第二批功能。
- **P2**：体验、工程化或长期维护优化。
- **P3**：可选探索，不阻塞核心可用性。

## 当前基线

- 仓库：<https://github.com/kiwi4814/totp-autofill-extension>
- 可见性：Public
- License：MIT
- 当前版本：`0.1.0`
- Chrome 权限：`activeTab`、`scripting`、`storage`
- 安全边界：本地生成、本地存储、不联网、不声明 `host_permissions`
- 当前已实现：导入、生成、复制、自动匹配、当前域名绑定、基础条目编辑、常规/多格 OTP 填充

---

## P0：下一批建议优先优化

### 1. Popup 智能主页

**目标**：让 Popup 不再只是简单列卡片，而是根据当前状态给出最直接动作。

建议状态：

- 当前网站已匹配 1 个条目：突出“填充当前网站”和“复制”。
- 当前网站匹配多个条目：显示推荐排序和快速搜索。
- 当前网站无匹配：进入“选择条目并绑定当前网站”的引导模式。
- 没有任何条目：突出“去导入”。
- 当前页面不支持读取域名或注入：提示原因和复制兜底。

验收标准：

- 用户打开 Popup 后，首屏就知道下一步该点什么。
- 未匹配时不再直接无上下文地堆所有条目。
- 保持现有权限不变。

### 2. 未匹配时的搜索选择绑定

**目标**：当前网站未匹配时，让用户可以快速搜索并绑定正确条目。

建议功能：

- Popup 内搜索 issuer/account/domain。
- 选择条目后绑定当前域名。
- 绑定后立即填充。
- 显示“以后将自动匹配 example.com”。

验收标准：

- 20+ 条目时仍能快速找到正确条目。
- 绑定行为明确、可理解、可撤销。

### 3. 填充失败后的智能兜底

**目标**：减少“没反应”的挫败感。

建议功能：

- 填充失败时显示原因：没有输入框、页面不允许注入、当前标签页不可用等。
- 始终保留“复制验证码”按钮。
- 显示“你可以手动粘贴验证码”。
- 后续可加入“选择页面输入框后填充”的手动模式。

验收标准：

- 自动填充失败时，用户仍然可以完成 2FA。
- 错误提示不暴露 secret。

### 4. 设置页信息架构重做

**目标**：把当前偏表单堆叠的设置页改成更适合管理大量条目的结构。

建议布局：

- 顶部：导入入口和安全状态。
- 左侧：条目列表 / 搜索 / 筛选。
- 右侧：当前条目的详情编辑。
- 底部或折叠区：清空全部等危险操作。

验收标准：

- 编辑条目时不需要滚动大量卡片。
- 删除和清空全部有更明确的二次确认。

---

## P1：高价值功能增强

### 5. 导入向导

**目标**：让导入流程更可控，避免重复导入和误覆盖。

建议步骤：

1. 选择来源：Aegis JSON / Google Authenticator / otpauth URI / QR 图片。
2. 输入或上传内容。
3. 预览将导入的条目。
4. 标记：新增、覆盖、重复、无效。
5. 用户确认后写入本地存储。

验收标准：

- 导入前能看到影响范围。
- 重复条目不会静默覆盖。
- 单条失败不影响其他有效条目导入。

### 6. 可选主密码 / 加密存储

**目标**：在保持“便利模式”的同时，提供更安全的本地 secret 保护。

建议设计：

- 默认仍为当前便利模式。
- 用户可选择开启主密码。
- 使用 WebCrypto 派生密钥并加密 secret。
- 解锁后短时缓存，不长期保存主密码。
- 支持已有条目迁移到加密存储。
- 支持关闭加密存储时明确风险确认。

验收标准：

- 不破坏当前用户数据。
- 主密码不会保存到 storage。
- 错误密码不会损坏已有数据。

### 7. 导出 / 备份

**目标**：避免误删或换设备时无法恢复。

建议功能：

- 导出为本项目 JSON 格式。
- 可选导出加密备份。
- 导入本项目备份。
- 导出前明确提示 secret 敏感性。

验收标准：

- 用户可以完整备份和恢复 issuer/account/secret/domains。
- 明文导出必须有风险提示。

### 8. 更强的填充能力

**目标**：覆盖更多真实网站 2FA 页面。

建议功能：

- Shadow DOM 输入框探测。
- iframe 内输入框探测（在权限允许范围内）。
- 用户手动选择输入框后填充。
- 更细的候选输入框评分解释。
- 针对常见站点的无权限本地启发式优化。

验收标准：

- 不增加长期 host permissions。
- 自动失败时有手动兜底。

### 9. 最近使用和使用频率

**目标**：让常用条目更容易触达。

建议功能：

- 记录最近填充时间。
- 记录当前域名最近使用的条目。
- Popup 排序优先显示最近和常用。

验收标准：

- 不记录页面路径，只记录域名级别信息。
- 用户可清除使用历史。

---

## P2：UI / UX 体验优化

### 10. Popup 视觉重做

建议方向：

- 更清晰的当前网站标题区。
- 主按钮更突出：填充 / 复制 / 绑定并填充。
- 倒计时改为进度环或进度条。
- 低剩余时间时提示“即将刷新”。
- 多条目时使用紧凑列表 + 展开详情。

### 11. 设置页视觉重做

建议方向：

- 从“卡片堆叠”改成“列表 + 详情面板”。
- 安全状态单独展示。
- 导入、管理、危险操作分区。
- 更清晰的空状态和错误状态。

### 12. 空状态和错误状态文案

建议状态：

- 未导入任何条目。
- 当前网站无匹配。
- 填充脚本注入失败。
- 找不到验证码输入框。
- 浏览器不支持 QR 识别。
- Aegis 密码错误。

验收标准：

- 每个状态都告诉用户“下一步可以做什么”。

### 13. 国际化 / 语言整理

建议功能：

- 统一中文文案。
- 可选英文界面。
- 后续如果上架 Chrome Web Store，补充 `/_locales`。

---

## P2：测试和工程化

### 14. 拆分并测试 content autofill 逻辑

当前 `src/core/autofill-planner.js` 已覆盖部分策略，但 `src/content/autofill.js` 仍有浏览器 DOM 逻辑未被自动化验证。

建议：

- 继续把候选评分和填充策略抽为纯函数。
- 增加 DOM fixture 测试或浏览器端测试。
- 覆盖多格输入、password OTP、隐藏输入框、disabled/readOnly 等场景。

### 15. 增加浏览器端 smoke test

建议工具方向：

- Playwright 或 Chrome extension test harness。
- 本地测试页面模拟常见 OTP 表单。
- 验证 Popup -> 注入脚本 -> 填充表单闭环。

注意：引入测试依赖前应单独评估，不影响扩展运行时代码零依赖原则。

### 16. CI

建议 GitHub Actions：

- `npm test`
- `npm run check`
- 可选：打包产物校验

验收标准：

- PR / push 后自动验证核心逻辑。

### 17. 打包和发布流程

建议：

- 增加 `npm run package` 生成 zip。
- 校验 zip 不包含 `.git`、`.omx`、`.DS_Store`、测试临时文件。
- 后续准备 Chrome Web Store 上架材料。

---

## P2：安全和隐私增强

### 18. 安全状态面板

建议在设置页显示：

- 是否联网：否。
- 是否声明 host permissions：否。
- Secret 存储方式：明文 local / 加密 local。
- 最近备份状态。
- 当前风险提示。

### 19. 数据清理能力

建议功能：

- 清除所有条目。
- 清除使用历史。
- 清除域名绑定。
- 清除导入暂存状态。

### 20. 敏感信息显示策略

建议：

- 默认不显示 secret。
- 需要显示时二次确认。
- 复制 secret 和导出备份时明确风险提示。

---

## P3：可选探索

### 21. 自动站点建议

根据 issuer/account/domain 文本，给出可能匹配的域名建议。必须保持本地计算，不使用联网查询。

### 22. 图标和品牌视觉

建议补充：

- 扩展图标。
- Popup 小尺寸视觉适配。
- README 截图或 GIF。

### 23. Manifest 版本和发布元信息

建议：

- 更新 `version` 策略。
- 增加 changelog。
- 整理 release notes。

---

## 推荐下一步实施顺序

1. Popup 智能主页。
2. 未匹配时搜索选择绑定。
3. 填充失败后的智能兜底。
4. 设置页信息架构重做。
5. 导入向导。
6. 可选主密码 / 加密存储。
7. 浏览器端 smoke test 和 CI。
